spring.profiles.active=test

spring.jpa.open-in-view=false

aws.access_key_id=${AWS_KEY}
aws.secret_access_key=${AWS_SECRET}
s3.bucket=${BOAKCATALOG_BUCKET_NAME}
s3.region=${BOAKCATALOG_BUCKET_REGION}
